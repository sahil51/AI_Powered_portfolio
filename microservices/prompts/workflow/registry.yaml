version: "1.0"
registry_version: "1.0.0"
prompts:
  - name: workflow.routing
    category: workflow
    version: 1.0.0
    status: active
    description: Workflow routing prompt
    tags: [workflow, routing]
    owner: platform
    variables:
      - name: message
        required: true
      - name: intent
        required: true
      - name: conversation_state
        required: true
