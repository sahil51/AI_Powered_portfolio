version: "1.0"
registry_version: "1.0.0"
prompts:
  - name: system.system
    category: system
    version: 1.0.0
    status: active
    description: Base system prompt for the AI executive assistant persona
    tags: [system, persona, core]
    owner: platform
    variables:
      - name: user_name
        required: false
        default: "Visitor"
      - name: current_date
        required: false
